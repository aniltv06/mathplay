# MathPlay 🎉

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![GitHub](https://img.shields.io/badge/GitHub-aniltv06%2Fmathplay-blue)](https://github.com/aniltv06/mathplay)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.x-blue)](https://www.typescriptlang.org/)

An interactive, gamified math practice web app for kids to practice addition, subtraction, multiplication, and division. Features a kid-friendly interface with animations, sound effects, streaks, and **automatic progress tracking** with user profiles.

Built with **TypeScript** + **Vite** for fast development and optimized production builds, with full **PWA** support.

## Features

- **User Profiles with Progress Tracking**: Automatically saves and restores your progress
- **Interactive Number Pad**: Touch-friendly input for iPad and mobile devices (supports up to 6-digit answers)
- **Customizable Worksheets**: Adjust number range, problem count, and operation types
- **Smart Problem Generation**: No duplicate questions, properly scaled division problems
- **Gamification**: Streak counter, confetti celebrations, progress bar, and score tracking
- **Persistent Storage**: Your answers and progress are saved automatically and restored when you return
- **Session History**: Track all your practice sessions with detailed statistics
- **PWA Support**: Install as an app on any device, works offline
- **Responsive Design**: Optimized for iPad, tablets, phones, and desktop
- **Audio Feedback**: Sound effects for correct/incorrect answers
- **Visual Feedback**: Animated responses and color-coded results

## Project Structure

```
mathplay/
├── index.html              # Main HTML file
├── src/
│   ├── main.ts            # TypeScript application logic
│   ├── types.ts           # TypeScript type definitions
│   ├── storage.ts         # localStorage utility for profiles
│   └── style.css          # All styles and animations
├── public/
│   ├── icons/             # PWA icons and favicons
│   ├── manifest.json      # PWA manifest (auto-generated by Vite)
│   └── sw.js              # Service worker (backup, Vite generates main one)
├── package.json           # Dependencies and scripts
├── vite.config.js         # Vite configuration with PWA plugin
├── vercel.json            # Vercel deployment config
└── README.md              # This file
```

## Quick Start

### Prerequisites

- Node.js 18+ installed
- npm, yarn, or pnpm package manager

### Installation

```bash
# Install dependencies
npm install
# or
yarn install
# or
pnpm install
```

### Development

```bash
# Start development server (opens at http://localhost:3000)
npm run dev
```

This starts Vite's dev server with:
- Hot Module Replacement (HMR)
- Instant updates on file changes
- Fast refresh

### Build for Production

```bash
# Build optimized production bundle
npm run build
```

Output will be in the `dist/` directory:
- Minified and optimized JavaScript
- Optimized CSS
- Service worker for offline support
- PWA manifest auto-generated

### Preview Production Build

```bash
# Preview production build locally
npm run preview
```

Opens at http://localhost:4173

## Deployment Options

This project is configured for **both Vercel and GitHub Pages**!

### Option 1: Vercel (Recommended for Production)

#### Auto-Deploy via GitHub Integration

1. **Push to GitHub** (see below)
2. **Visit** [vercel.com](https://vercel.com)
3. **Import** your `aniltv06/mathplay` repository
4. **Deploy** - Vercel auto-detects Vite and deploys
5. **Result**: `https://mathplay.vercel.app`

**No configuration needed!** Vercel automatically:
- Runs `npm run build`
- Deploys the `dist/` folder
- Provides HTTPS and global CDN
- Auto-deploys on every git push to main

#### Manual Deploy via CLI

```bash
# Install Vercel CLI
npm i -g vercel

# Deploy to production
npm run deploy

# Or step by step:
vercel --prod
```

### Option 2: GitHub Pages (Automatic)

**Already configured!** Just push to GitHub:

```bash
git push origin main
```

GitHub Actions will automatically:
1. ✅ Build your app
2. ✅ Deploy to GitHub Pages
3. ✅ Available at: `https://aniltv06.github.io/mathplay/`

#### Enable GitHub Pages (One-Time Setup)

1. Go to repo **Settings** → **Pages**
2. **Source**: Deploy from a branch
3. **Branch**: `gh-pages` (will be created automatically)
4. Click **Save**

**That's it!** Every push to `main` triggers auto-deployment.

### Deployment Comparison

| Feature | Vercel | GitHub Pages |
|---------|--------|--------------|
| **URL** | mathplay.vercel.app | aniltv06.github.io/mathplay |
| **Setup** | GitHub integration | Automatic workflow |
| **Build Time** | ~30 seconds | ~1-2 minutes |
| **CDN** | Global | Global |
| **HTTPS** | Auto | Auto |
| **Custom Domain** | Free | Free |
| **Auto Deploy** | Yes (on push) | Yes (on push) |
| **Analytics** | Built-in | Via GitHub |

### Recommended Strategy

1. **Primary**: Vercel (faster, better for PWAs)
2. **Backup**: GitHub Pages (always available)
3. **Custom Domain**: Point to Vercel

---

## Quick Start (Original sections continue...)

```bash
# Install gh-pages
npm install -D gh-pages

# Add to package.json scripts:
# "deploy:gh": "vite build --base=/your-repo-name/ && gh-pages -d dist"

# Build and deploy
npm run deploy:gh
```

**Important**: Set the `base` option in `vite.config.js`:
```javascript
export default defineConfig({
  base: '/your-repo-name/',
  // ... rest of config
});
```

### Option 3: Netlify

#### Method A: Netlify CLI

```bash
# Install Netlify CLI
npm install -g netlify-cli

# Build
npm run build

# Deploy
netlify deploy --prod --dir=dist
```

#### Method B: Netlify Drag & Drop

1. Run `npm run build`
2. Visit [app.netlify.com/drop](https://app.netlify.com/drop)
3. Drag the `dist/` folder

### Option 4: Cloudflare Pages

1. Push to GitHub
2. Visit [pages.cloudflare.com](https://pages.cloudflare.com)
3. Connect repository
4. Build settings:
   - Build command: `npm run build`
   - Output directory: `dist`

## PWA Installation

Once deployed, users can install the app:

### On Mobile (iOS/Android)
1. Visit the website
2. Browser menu → "Add to Home Screen"
3. App appears on home screen with icon

### On Desktop (Chrome/Edge)
1. Visit the website
2. Click install icon in address bar
3. Or Chrome menu → "Install Math Fun"

### On iPad
1. Open in Safari
2. Share button → "Add to Home Screen"
3. Opens in fullscreen mode

## PWA Features

- **Offline Support**: App works without internet after first visit
- **Installable**: Add to home screen like a native app
- **Fast Loading**: Vite-optimized bundle + service worker caching
- **Background Sync**: Ready for future enhancements
- **App-like Experience**: Fullscreen, no browser UI
- **Auto-updates**: Service worker automatically updates

## Progress Tracking

### How It Works

MathPlay automatically saves your progress using browser localStorage. Everything persists across page reloads and browser restarts!

**What's Saved:**
- ✅ User profile with your name
- ✅ Current practice session (problems and answers)
- ✅ All completed sessions with timestamps
- ✅ Statistics (total problems, accuracy, best streak)
- ✅ Settings preferences

**Auto-Save Triggers:**
- When you answer a question
- When you move to the next question
- When you close the number pad
- When you complete all problems

**Session Restoration:**
- Enter your name → automatically loads your last session
- If you were in the middle of practice, it restores:
  - All the same problems
  - Your previous answers
  - Your progress and stats

### Profile Management

Each profile includes:
- **Session History**: All past practice sessions
- **Statistics**:
  - Total sessions completed
  - Total problems attempted
  - Total correct/wrong answers
  - Best streak achieved
  - Average percentage
  - Total time spent
- **Current Session**: Ongoing worksheet (if any)

### Data Privacy

All data is stored **locally in your browser** using localStorage:
- ✅ No server uploads
- ✅ No accounts required
- ✅ No internet needed after first load
- ✅ Complete privacy
- ✅ Clear data anytime via browser settings

## Development Commands

```bash
# Install dependencies
npm install

# Start dev server (http://localhost:3000)
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview

# Deploy to Vercel
npm run deploy

# Type checking (if using TypeScript)
npm run type-check

# Lint code (add ESLint config)
npm run lint
```

## Customization

### Changing Colors

Edit `src/style.css`:
```css
/* Main gradient */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Change to your colors */
background: linear-gradient(135deg, #yourcolor1 0%, #yourcolor2 100%);
```

### Changing App Name

1. Edit `vite.config.js`:
   ```javascript
   VitePWA({
     manifest: {
       name: 'Your App Name',
       short_name: 'Your App'
     }
   })
   ```
2. Edit `index.html`:
   ```html
   <title>Your App Name</title>
   ```

### Adding More Features

The code is modular, typed, and commented. Key functions in `src/main.ts`:
- `generateProblems()` - Problem generation logic with unique pairs
- `validateAnswer()` - Single answer validation
- `updateStats()` - Real-time statistics calculation
- `saveProgress()` - Auto-save to localStorage
- `playSound()` - Audio feedback
- `createConfetti()` - Celebration animations

Key modules:
- `src/types.ts` - TypeScript type definitions
- `src/storage.ts` - localStorage utilities for profiles

## Configuration Files

### vite.config.js
- Vite PWA plugin configuration
- Build optimization settings
- Dev server settings
- Asset handling

### package.json
- Dependencies and versions
- NPM scripts
- Project metadata

### vercel.json
- Vercel deployment settings
- Headers and rewrites
- Security headers

## Browser Support

- Chrome/Edge: Full support
- Safari: Full support (iOS 11.3+)
- Firefox: Full support
- Opera: Full support

## Performance

- **Lighthouse Score**: ~95-100
- **First Contentful Paint**: < 0.5s (with Vite)
- **Time to Interactive**: < 1s
- **Bundle Size**: ~14KB gzipped (TypeScript + CSS + localStorage)
- **Build Time**: < 5s

## Tech Stack

- **Language**: TypeScript 5.x
- **Build Tool**: Vite 5
- **PWA**: vite-plugin-pwa
- **Storage**: Browser localStorage API
- **CSS**: Pure CSS (no frameworks)
- **Deployment**: Vercel/Netlify/GitHub Pages

## Troubleshooting

### Port Already in Use
```bash
# Kill process on port 3000
lsof -ti:3000 | xargs kill -9

# Or change port in vite.config.js
server: { port: 3001 }
```

### Build Errors
```bash
# Clear cache and reinstall
rm -rf node_modules dist .vite
npm install
npm run build
```

### Service Worker Not Updating
```bash
# Rebuild and hard refresh
npm run build
# In browser: Ctrl+Shift+R (or Cmd+Shift+R on Mac)
```

## License

MIT License - Free to use, modify, and distribute.

## Author

**Anil Kumar Thatha Venkatachalapathy**
Email: [aniltv06@gmail.com](mailto:aniltv06@gmail.com)

## Contributing

Feel free to:
- Add new features
- Improve UI/UX
- Fix bugs
- Enhance accessibility
- Add translations

## Support

For issues or questions:
1. Check existing issues
2. Create new issue with details
3. Include browser/device info

## Roadmap

**Completed Features:**
- [x] TypeScript migration
- [x] User profiles with progress tracking
- [x] Smart problem generation (no duplicates, proper division scaling)
- [x] 6-digit answer support for advanced math

**Potential Future Enhancements:**
- [ ] Profile selector UI with statistics preview
- [ ] Multiple difficulty levels
- [ ] Timed challenges
- [ ] Printable worksheet generation
- [ ] Parent dashboard with analytics
- [ ] Multi-language support
- [ ] Voice feedback option
- [ ] Achievement badges
- [ ] Leaderboards
- [ ] Cloud sync (optional)

---

**Made with ❤️ for young learners**

Enjoy teaching math! 🎓✨
